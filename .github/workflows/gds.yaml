name: gds

on:
  push:
  workflow_dispatch:

jobs:
  gds:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v4
        with:
          submodules: recursive
          
      - name: symb_link_extra_step
        run: |
          mkdir -p  /home/runner/work/runs/wokwi/reports/signoff/31-sta-rcx_nom \
          && mkdir -p  /home/runner/work/runs/wokwi/reports/signoff/30-sta-rcx_nom \
          && ln -s  /home/runner/work/runs/wokwi/reports/signoff/31-sta-rcx_nom/* /home/runner/work/runs/wokwi/reports/signoff/30-sta-rcx_nom
          
      - name: Build GDS
        uses: TinyTapeout/tt-gds-action@tt06
  
  precheck:
    needs: gds
    runs-on: ubuntu-latest
    steps:
      - name: Run Tiny Tapeout Precheck
        uses: TinyTapeout/tt-gds-action/precheck@tt06

  gl_test:
    needs: gds
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: GL test
        uses: TinyTapeout/tt-gds-action/gl_test@tt06

  viewer:
    needs: gds
    runs-on: ubuntu-latest
    permissions:
      pages: write      # to deploy to Pages
      id-token: write   # to verify the deployment originates from an appropriate source
    steps:
      - uses: TinyTapeout/tt-gds-action/viewer@tt06
